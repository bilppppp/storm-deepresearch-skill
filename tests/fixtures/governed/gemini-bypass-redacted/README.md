# Gemini Bypass Redacted Fixture

This fixture is a minimal, redacted incident descriptor derived from reviewer-confirmed failure modes. It intentionally excludes raw private output and keeps only reproducible shortcut patterns, earliest responsible stage, and expected non-zero result.

The executable assertions live in `tests/test_incident_regressions.py`.
